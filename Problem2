#include<stdio.h>
int main()
{
int n,Sum=0;
scanf("%d",&n);
int arr[n];
for(int i=0; i<n; i++)
{
    scanf("%d",&arr[i]);
}
for(int i=0; i<n; i++)
{
    Sum+=arr[i];
}
printf("Sum is: %d",Sum);
    return 0;
}